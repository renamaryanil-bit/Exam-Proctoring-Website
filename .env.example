# Copy this file to .env and fill in your values
# The proxy setting in package.json forwards /api to the backend automatically
# so this value is usually fine as-is for local development.
REACT_APP_API_URL=/api
